name: Maintainer CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  quality:
    name: Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.12", "3.14"]

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install maintainer dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install --group dev

      - name: Verify course structure
        run: python scripts/verify_course.py

      - name: Run maintainer tests
        run: pytest

      - name: Lint maintainer code
        run: ruff check scripts tests projects/vuacoc-bot-journey
