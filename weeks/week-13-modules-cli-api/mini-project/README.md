# Mini-project — Local Bot CLI and Adapter Boundary

    CLI → course-local adapter → Bot Core

Chạy:

    python weeks/week-13-modules-cli-api/cli.py --position 1 --goal 4
    python projects/vuacoc-bot-journey/local_arena/cli.py --bot-a student --bot-b wait

Core không phụ thuộc CLI hay network. HTTP lesson dùng neutral static fixture.
ARENA_ADAPTER_STATUS = DESIGN_ONLY và VUACOC_RUNTIME_CONTRACT = UNVERIFIED.
